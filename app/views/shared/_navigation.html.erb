<nav class="navbar navbar-expand-lg navbar-light bg-primary margin:10">
  <div class="container-fluid">
    <a class="navbar-brand" href="#">E-Commerce</a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarTogglerDemo02" aria-controls="navbarTogglerDemo02" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarTogglerDemo02">
      <ul class="navbar-nav me-auto mb-2 mb-lg-0">
        <li class="nav-item">
        <%= link_to "Home", categories_path, class: 'btn btn-primary', role: 'button' %>
        </li>
        <li class="nav-item">
        <%= link_to "Profile", profile_index_path, class: 'btn btn-primary', role: 'button' %>
        </li>
        <li class="nav-item">
        <%= link_to "My Order", orders_path, class: 'btn btn-primary', role: 'button' %>
        </li>
      </ul>

      <div class="d-flex">
      <% if user_signed_in? %>
        <div class="mt-1"> Welcome <%= current_user.email %> </div>
        <span class="mx-2"></span>
        <%= button_to "Sign out", destroy_user_session_path, method: :delete, class: "btn btn-danger rounded-pill" %>
      <% else %>
        <%= button_to "Sign in", new_user_session_path, class: "btn btn-primary rounded-pill" %>
      <% end %>
    </div>
    

    
      </div>
    </div>
  </div>
</nav>